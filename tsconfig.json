{
  // --------------------------------------------------------------
  // tsconfig.json – Next.js + TypeScript (Path Alias พร้อมใช้งาน)
  // --------------------------------------------------------------
  "compilerOptions": {
    /* ---------- JS / TS ระดับมาตรฐาน ---------- */
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],

    /* ---------- พฤติกรรมคอมไพเลอร์ ---------- */
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "allowJs": true,
    "incremental": true,

    /* ---------- โมดูล / import ---------- */
    "module": "esnext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,

    /* ---------- React / JSX ---------- */
    "jsx": "preserve",

    /* ---------- Path Alias (สำคัญสุด) ---------- */
    "baseUrl": ".",                         // อ้าง root = project root
    "paths": {
      "@/*": ["src/*"],                     // ใช้ @/… ชี้ไปยัง src ทั้งหมด
      "@/components/*": ["src/components/*"],
      "@/lib/*": ["src/lib/*"]
    },

    /* ---------- Next.js plugin ---------- */
    "plugins": [{ "name": "next" }]
  },

  /* ---------- Scope ไฟล์ที่ต้อง compile ---------- */
  "include": [
    "next-env.d.ts",
    "src",               // รวมทุกไฟล์ใน src/
    "src/types/**/*",    // custom type declaration
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  "exclude": ["node_modules"]
}
